#include "main.h"

/**
 * _abs - computes absolute value of an int
 *@n: interger to check
 *
 * Return: absolute value on integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);

}
